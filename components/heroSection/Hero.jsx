import React from "react";
import Sparkles from "@/components/heroSection/Sparkles";

const Hero = ({ products }) => {
  return (
    <>
      <Sparkles products={products} />
    </>
  );
};

export default Hero;
